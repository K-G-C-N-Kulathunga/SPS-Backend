spring.profiles.active=dev
spring.application.name=SPS
server.port=9090

# Use JNDI datasource from WildFly
spring.datasource.jndi-name=java:jboss/datasources/OrcleDatasourceSMC

# Hibernate Dialect
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect


spring.sql.init.mode=never

# Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework.data.jpa=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicTypeDescriptor=TRACE

server.servlet.context-path=/sps-3.3.5
logging.level.org.springframework.web=DEBUG

upload.dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thanuksha@gmail.com
spring.mail.password=jvib gttp ubei iyhj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# 2. Shows the actual values inside the '?' parameters (Crucial for debugging)
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE