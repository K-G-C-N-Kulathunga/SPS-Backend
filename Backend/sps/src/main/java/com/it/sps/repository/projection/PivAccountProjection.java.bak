package com.it.sps.repository.projection;

public interface PivAccountProjection {

    // Must exactly match query alias
    String getCodeNo();        // matches AS codeNo
    String getDescription();   // matches AS description
    BigDecimal getAmount();    // matches AS amount, numeric type
}